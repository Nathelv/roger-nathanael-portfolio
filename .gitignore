# Environment files (never commit real secrets)
.env
.env.local
.env.*.local

# Vercel
.vercel

# Dependencies (none required, but ignore if added later)
node_modules/

# OS / editor noise
.DS_Store
Thumbs.db
